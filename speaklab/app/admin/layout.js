import "./admin.css";

export const metadata = {
  title: "لوحة التحكم | speakLab",
  description: "لوحة تحكم إدارة التسجيلات — مدرسة speakLab",
};

export default function AdminLayout({ children }) {
  return children;
}
